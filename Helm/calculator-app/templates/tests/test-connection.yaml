apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculator-app.fullname" . }}-test-connection"
  labels:
    {{- include "calculator-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calculator-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never